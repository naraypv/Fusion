# Isolated HyperResearch durable-store proof

This branch contains only three generic Quint files for the immutable revision protocol. It contains no LifeOS implementation, configuration, credentials, project data, vault data, or Akashic data. The workflow checks their exact SHA-256 values, verifies the positive model, and requires the same safety properties to reject an overwrite mutant.
